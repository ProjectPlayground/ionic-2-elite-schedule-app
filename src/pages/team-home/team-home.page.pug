ion-header
  ion-navbar(color='primary')
    ion-title
      | {{team.name}}
    ion-buttons(end)
      button((click)='goHome()', ion-button)
        ion-icon(name='home')
ion-tabs
  ion-tab(tabTitle='Team', [root]='teamDetailTab', [rootParams]='team', tabIcon='basketball')
  ion-tab(tabTitle='Standings', [root]='standingsTab', [rootParams]='team', tabIcon='podium')